<!DOCTYPE html>
<html>

<head>
    <title>Add Medication</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>

    <div class="container mt-5">

        <h2>Add Medication to Tracker</h2>

        <hr>

        <form action="/addMedication" method="POST">

            <select name="medicationId" class="form-select mb-3" required>
                <option value="">Select Medication</option>
                <% medications.forEach(medication => { %>
                    <option value="<%= medication.medicationId %>"><%= medication.medicationName %></option>
                <% }); %>
            </select>

            <div class="mb-3">

                <label class="form-label">
                    Dosage *
                </label>
                <input type="text" name="dosage" class="form-control" placeholder="Enter dosage (e.g., 500mg)" required>
            </div>

            <div class="mb-3">

                <label class="form-label">
                    Frequency *
                </label>
                <input type="text" name="frequency" class="form-control"
                    placeholder="Enter frequency (e.g., 2 times a day)" required>
            </div>

            <div class="mb-3">

                <label class="form-label">
                    Start Date *
                </label>
                <input type="date" name="startDate" class="form-control" required>
            </div>

            <div class="mb-3">

                <label class="form-label">
                    End Date 
                </label>
                <input type="date" name="endDate" class="form-control">
            </div>

            <div class="mb-3">

                <label class="form-label">
                    Notes 
                </label>
                <textarea name="notes" class="form-control" placeholder="Enter any notes"></textarea>
            </div>

            <button type="submit" class="btn btn-primary">
                Add Medication
            </button>

            <a href="/dashboard" class="btn btn-secondary">
                Back
            </a>
        </form>

    </div>

</body>